dist: trusty  # репозиторий внутри travis
jdk: oraclejdk8
language: java
services:
  - postgresql
  - docker
befor_install: -chmod +x mvnw
install: mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V

